# Notebook-generated image assets

These images were extracted from `Q_Validate_Rigorous_Identity_Disjoint_Benchmarking_of_ZZ_Entangled_Quantum_Kernels.ipynb`.

## Provenance and use restriction

The notebook itself uses synthetic random features and labels. It runs a classical RBF SVM, includes simulated hyperparameter scores, and contains illustrative fallback values in its graph/reporting cell. Therefore, these images are **synthetic pipeline demonstrations only**. They must not be presented as FaceForensics++ results, measured quantum-kernel performance, KTA findings, effective-rank findings, or evidence of quantum advantage.

Use captions such as:

> Synthetic pre-flight visualisation generated during pipeline validation; not a FaceForensics++ result.

Before using any image in the article, regenerate it from the lawful FaceForensics++ ArcFace embedding file and the identity-disjoint quantum benchmark. Preserve the result CSV, split metadata, environment versions, and code commit alongside the regenerated image.

## Extracted files

The two PNG files in this directory are the embedded image outputs found in notebook cell 6. Their names include a short SHA-256 content hash for traceability.
